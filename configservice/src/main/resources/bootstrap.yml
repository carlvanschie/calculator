server:
  port: 8888
spring:
  cloud:
    config:
      server:
        native:
          searchLocations: file://${user.home}/work/microservices/config-repo
  application:
    name: config-service
  profiles:
    active: native
